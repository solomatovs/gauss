[package]
name = "codec-json"
version = "0.1.0"
edition = "2024"

[lib]
name = "codec_json"
crate-type = ["cdylib"]

[dependencies]
server-api = { path = "../../../libs/api" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[package]
name = "storage-memory"
version = "0.1.0"
edition = "2024"

[dependencies]
server-api = { path = "../../../libs/api" }
tokio = { version = "1", features = ["sync"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

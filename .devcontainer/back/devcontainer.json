{
  "name": "back",
  "image": "python:3.11",
  "workspaceFolder": "/app",

  "mounts": [
    "source=${localWorkspaceFolder}/.git,target=${containerWorkspaceFolder}/.git,type=bind",
    "source=${localWorkspaceFolder}/.gitignore,target=${containerWorkspaceFolder}/.gitignore,type=bind",
    "source=${localWorkspaceFolder}/.vscode,target=${containerWorkspaceFolder}/.vscode,type=bind",
    "source=${localWorkspaceFolder}/.devcontainer,target=${containerWorkspaceFolder}/.devcontainer,type=bind",
    "source=${localWorkspaceFolder}/back,target=${containerWorkspaceFolder}/back,type=bind",
    "source=${localWorkspaceFolder}/front,target=${containerWorkspaceFolder}/front,type=bind"
  ],
  "postCreateCommand": "export DEBIAN_FRONTEND=noninteractive && apt update && apt -y install net-tools dnsutils telnet lsof make && cd ${containerWorkspaceFolder}/back && make setup",
  "waitFor": "postCreateCommand",
  "shutdownAction": "stopContainer",

  "containerEnv": {
    "PATHONPATH": "${containerWorkspaceFolder}/back"
  },

  "runArgs": ["--network", "gauss", "--name", "back"],
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": { "label": "back", "onAutoForward": "openBrowser" }
  },

  "hostRequirements": {
    "cpus": 1,
    "memory": "2gb",
    "storage": "2gb"
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "dracula-theme.theme-dracula",
        "formulahendry.auto-rename-tag",
        "naumovs.color-highlight",
        "intellsmi.comment-translate",
        "Continue.continue",
        "ms-python.python",
        "ms-python.debugpy",
        "ms-python.vscode-pylance",
        "ms-python.vscode-python-envs",
        "ms-vscode.extension-test-runner",
        "ms-playwright.playwright",
        "charliermarsh.ruff",
      ]
    }
  }
}

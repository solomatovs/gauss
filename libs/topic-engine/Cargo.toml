[package]
name = "topic-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
server-api = { path = "../api" }
tokio = { version = "1", features = ["sync", "rt"] }
serde_json = "1"
thiserror = "2"
tracing = "0.1"
